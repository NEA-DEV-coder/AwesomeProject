import { StyleSheet } from 'react-native';

const style = StyleSheet.create({
  storyContainer: {
    // marginLeft: 27,
    // alignItems: 'center',
    marginRight: 20,
  },
  firstName: {
    textAlign: 'center',
    fontSize: 14,
    fontWeight: 500,
    color: '#022150',
    marginTop: 8,
  },
});

export default style;
