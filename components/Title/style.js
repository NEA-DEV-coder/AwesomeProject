import { StyleSheet } from 'react-native';

const style = StyleSheet.create({
  title: {
    color: '#022150',
    fontFamily: 'Bold',
    fontWeight: '600',
    fontSize: 24,
  },
});

export default style;
